# Base URL of the LastCheck app (onboarding wizard). Defaults to http://localhost:3000
NEXT_PUBLIC_APP_ORIGIN=http://localhost:3000

# GitHub Pages project site only: must match /your-repo-name (see landing/README.md).
# Leave unset for local dev; CI sets this automatically.
# BASE_PATH=/lastCheck
